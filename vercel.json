{
  "buildCommand": "cd aiconference && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "aiconference/dist",
  "installCommand": "cd aiconference && npm install --legacy-peer-deps",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
